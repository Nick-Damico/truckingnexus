<!DOCTYPE html>
<html class='h-full'>
  <head>
    <title>Trucking Nexus | Users Portal</title>
    <meta name='viewport' content='width=device-width,initial-scale=1'>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'tailwind', 'inter-font', 'data-turbo-track': 'reload' %>
    <%= stylesheet_link_tag 'application', 'data-turbo-track': 'reload' %>
    <%= javascript_importmap_tags %>
  </head>
  <body class='h-full bg-gray-200'>
    <div class='flex flex-col h-full'>

      <%= render 'shared/navbar' %>

      <div class='flex flex-col px-5 grow'>

        <%= render 'shared/flash_and_error_messages' %>

        <main class='h-full'>
          <div class='relative flex md:h-full md:flex-row gap-8 justify-end md:justify-center'>

            <div class='absolute z-40 md:relative -left-[275px] has-[:checked]:left-0 md:left-0 top-0 md:h-full transition-all ease-in-out duration-300'>
              <%= render partial: 'side_navigation', locals: { user: @user } %>
            </div>

            <div class='flex flex-col w-11/12 md:w-4/5 gap-y-2'>
              <%= yield %>
            </div>

          </div>
        </main>
      </div>

      <%= render 'shared/footer' %>

    </div>
  </body>
</html>
